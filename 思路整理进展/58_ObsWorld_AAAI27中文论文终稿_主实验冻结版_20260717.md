---
title: ObsWorld AAAI-27 中文论文终稿（主实验冻结版）
aliases:
  - ObsWorld AAAI27 中文终稿
  - ObsWorld 论文母稿
tags:
  - ObsWorld
  - AAAI27
  - 论文终稿
  - EarthNet2021
  - PredictiveState
created: 2026-07-17
updated: 2026-07-17
status: 结构与论证已冻结｜实验数值待填｜后续直接修改本文
related:
  - "[[52_ObsWorld_AAAI27中心叙事训练实验与写作总纲_20260716]]"
  - "[[56_ObsWorld_Stage2_200epoch_8卡训练计划与关键Checkpoint命令_20260717]]"
  - "[[57_ObsWorld_Stage2_下一步建议与physical4_full24_NAS启动调查_20260717]]"
---

# 58. ObsWorld AAAI-27 中文论文终稿（主实验冻结版）

> [!important] 使用说明
> 这是后续唯一的论文母稿。已确认的代码事实、方法流程、实验设计和公开参考数值已经写入；尚未产生的本项目结果统一标成 **【待填】**，不得用预期值代替。以后直接修改本文，不再新建另一份“终稿”。

> [!warning] 当前投稿口径
> 当前执行 no-U（没有正式观测更新器）版本。正文核心是 shared-transition predictive state（共享转移预测状态）。Stage1.5 在预训练时使用真实 φ，但 EarthNet Stage2 使用固定 neutral φ（中性条件），因此“实时采集条件感知”不是当前主标题。U 只有完成端到端训练并通过强基线门槛后，才升级标题、摘要、Table 2 和 Figure 3。

> [!note] 数据口径
> 论文阶段暂称 EarthNet2021。Stage1/1.5 的预训练数据是 SSL4EO-S12 v1.1，Stage2 的预测数据是 EarthNet2021；这是预训练后适配，不是两个主数据集。服务器路径中的 earthnet2021x 是工程目录名，不在本文扩展新的数据集叙事。

---

# 论文首页信息

## 当前主标题

> **ObsWorld: A Shared-Transition Predictive-State World Model for Long-Horizon Earth Observation Forecasting**

中文：

> **ObsWorld：面向长期地球观测预测的共享转移预测状态世界模型**

## U 通过后才允许使用的升级标题

> **ObsWorld: An Observation-Correctable Predictive-State World Model for Earth Observation Forecasting**

中文：

> **ObsWorld：面向地球观测预测的可由观测校正的预测状态世界模型**

## 不使用的标题词

正文和标题暂不使用：

- digital twin（数字孪生）；
- causal Earth simulator（因果地球模拟器）；
- full physical simulator（完整物理模拟器）；
- first weather-driven EO world model（首个天气驱动地球观测世界模型）。

原因是当前任务只预测可由卫星验证的地表观测，且已有 Contextformer、EO-WM、VegSim 等相关工作使用气象驱动或世界模型定位。

---

# 摘要

## 当前 no-U 版摘要

卫星遥感能够长期记录地表变化，但观测稀疏、受云遮挡影响，地表演化还持续受到气象、季节与地理背景影响。长期地球观测预测因此不仅需要生成某个固定时距的未来图像，还需要维护一个能够在外生驱动下持续推进、并可被真实未来观测验证的内部状态。我们提出 ObsWorld，一个面向长期地球观测预测的共享转移预测状态世界模型。ObsWorld 将经过跨模态与真实采集条件 φ 预训练的编码器和状态投影器迁移到 EarthNet；由于 EarthNet Stage2 没有兼容的逐帧 φ，初始化器使用固定中性条件，而不假装获得真实采集元数据。随后，模型在降水、温度、饱和水汽压亏缺和太阳辐射等紧凑物理驱动下，复用共享五日状态转移进行 100 天开放循环递推，并将每一步状态解码为多光谱未来观测。为区分共享递推的贡献与普通多时距预测能力，我们构造严格配对的非递推 Direct 对照，使两者共享数据、预训练初始化、驱动、解码器和训练预算。我们在冻结的 EarthNet2021 协议上评估分布内与分布外预测。ObsWorld 相对 Persistence、Climatology、Contextformer、PredRNN/SimVP 和 Direct-P4 的结果为【待填】；时距分析表明【待填】；驱动扰动与初始化消融表明【待填】。这些结果支持的最强结论为【根据决策门填写：共享递推改善长期预测 / 共享递推在保持精度的同时提供统一状态过程 / 尚不足以支持递推优势】。

> [!note] 摘要填数规则
> 最后一句必须根据结果选择，不能默认写“显著优于”。如果 Rollout-P4 未超过或至少不劣于 Direct-P4，就不能在摘要中声称共享递推是性能贡献。

## U 通过后的摘要改动位置

仅在 Stage2-U 通过后，在方法句之后加入：

> 当中途获得部分有效的新观测时，ObsWorld 进一步根据可见区域内的预测—观测残差和观测陈旧度更新内部状态，再继续预测后续未来。

在结果句加入：

> 在第 25 天和第 50 天揭示新观测时，ObsWorld-U 相对容量匹配在线更新基线将后续误差降低【待填】，且在无有效观测时保持严格不更新。

U 未通过时不得出现以上两句。

---

# 1. 引言

## 1.1 现实问题

地球观测卫星不会连续看见同一块地表。观测之间存在重访间隔，云和无效像素会遮挡部分区域，成像结果还会受到太阳高度、轨道方向和传感器平台影响。与此同时，植被和地表状态会持续受到降水、温度、辐射、季节和地理背景影响。模型因而面临两个不同层次的问题：

1. 在没有新观测的日子里，能否根据已有状态和外部驱动持续预测；
2. 每一个未来状态能否还原成真实卫星可以检验的观测。

当前本文首先解决第一个问题，并把第二个问题作为观测空间中的验证闭环。中途新观测到来后的状态更新属于 Stage2-U 条件扩展，不在当前主结果中提前承诺。

## 1.2 现有方法的不足应怎样表述

已有方法并不是“不会预测未来”。EarthNet2021 推动了多光谱地表预测，Contextformer 将气象信息引入高分辨率植被预测，VegeDiff 进一步建模概率未来；最新 EO-WM 和 VegSim 已经把任务表述为天气或情景驱动的世界模型。因此本文不能把“使用天气”或“叫作世界模型”本身当作贡献。

当前更具体的缺口是：

> 许多方法直接输出整个未来序列或分别预测不同终点，却不一定显式检验：是否存在一个由预训练历史编码器初始化、并由同一短步转移反复推进的预测状态；以及这种共享状态过程在长期时距和分布变化下是否优于严格配对的非递推预测。

这句话与当前可执行的 Direct-P4 对 Rollout-P4 实验一一对应。

## 1.3 核心想法

ObsWorld 把长期预测分成三个可检验算子：

- I，initializer（初始化器）：从历史多光谱观测与采集条件形成预测状态；
- F，controlled transition（受控状态转移）：在 physical4、季节和地理条件下推进状态；
- O，observation decoder（观测解码器）：把状态还原为未来 RGBN 卫星观测。

共享 F 每次推进五天，连续使用 20 次得到 100 天未来。Direct-P4 使用同一初始状态和相同未来驱动，但不把上一步预测状态递交给下一步，构成严格配对的非递推对照。

## 1.4 当前贡献草案

在结果产生前，贡献只写成可验证形式：

1. 我们提出一个 shared-transition predictive-state formulation（共享转移预测状态表述），将预训练状态初始化、长期受控递推和观测空间验证连接为同一过程；Stage1.5 的真实 φ 条件只属于预训练，Stage2 使用中性条件。
2. 我们设计 shared five-day controlled transition（共享五日受控转移），在紧凑 physical4 驱动下开放循环推进 100 天，并配套 matched non-recursive Direct control（严格配对的非递推 Direct 对照）。
3. 我们建立冻结的数据、检查点选择和 IID/OOD 评估协议，用时距曲线、驱动扰动与初始化消融判断共享状态、外部驱动和 Stage1.5 各自是否带来真实收益。
4. 只有 Stage2-U 通过后才增加：我们提出质量感知观测更新器，在部分有效新观测到来时安全校正状态。

> [!warning] 贡献与结果的绑定
> 第 1–3 条也需要结果支持。若 Rollout 明显弱于 Direct，第 2 条只能写成“提出并系统检验”，不能写成“有效提高”。

---

# 2. 相关工作

## 2.1 地球观测预测

EarthNet2021 将卫星图像预测组织为长期地表预测任务。后续 Contextformer 在 GreenEarthNet 兼容设置下结合空间上下文与气象时间序列，并报告 R²、RMSE、NSE、绝对偏差和相对气候态的超越比例。它是当前最接近本任务的强确定性基线。

本文与 Contextformer 的区别不应写成“我们使用天气、它不使用”，因为 Contextformer 明确使用天气。可写为：

> Contextformer 侧重高分辨率植被预测；ObsWorld 进一步用严格 Direct 对照检验共享短步状态转移是否能够构成统一、长期可验证的预测过程。

## 2.2 视频预测与递推模型

PredRNN、SimVP 和 Earthformer 分别代表递归视频预测、直接时空预测和基于注意力的地球系统预测。它们用于回答：ObsWorld 的收益是否只是更大模型或普通时空预测带来的。

主表至少同协议重跑 Contextformer 和 PredRNN；如果 PredRNN 适配成本不合理，可用 SimVP 替代，但必须记录选择理由。

## 2.3 概率地球观测与世界模型

VegeDiff 使用潜变量扩散建模植被预测的不确定性。EO-WM 使用物理信息条件与视频扩散，强调极端天气响应。VegSim 从稀疏 NDVI 历史和气象驱动中递推潜在植被状态，并支持气象情景模拟。

因此，本文不声称“首次天气驱动世界模型”。当前差异必须落在：

- 经过跨模态与真实 φ 条件训练的状态权重，以固定中性 φ 迁移到 Stage2；
- 观测空间中可验证的共享短步递推；
- 严格配对 Direct 对照；
- 如果 U 成功，再增加再观测后的状态更新。

## 2.4 表征去耦与采集条件

Stage1.5 预训练使用真实 φ 表示太阳角度、轨道和平台等采集条件，并通过 FiLM 进入预训练编码器与重建解码器。交叉协方差损失只能抑制特定相关性，不能证明所有非线性干扰都被去除。迁移到 EarthNet Stage2 时，由于缺少兼容的逐帧元数据，正式代码使用固定 neutral φ；因此本文采用“φ 条件化状态预训练”而非“Stage2 实时采集条件感知”或“完全采集不变”的表述，并通过预测效用和增量 probe 评价。

---

# 3. 方法

## 3.1 任务定义

每个 EarthNet2021 样本包含：

- 50 天历史上下文；
- 100 天未来目标；
- 未来被组织为 20 个五日窗口；
- 多光谱 RGBN 观测；
- 有效像素掩码；
- physical4 五日驱动；
- 日历、静态地理与地形信息；
- Stage2 的固定 neutral φ（中性缺失条件）；真实逐帧 φ 只在 Stage1.5 预训练数据中可用。

记历史观测为 x 的上下文序列，目标未来为 y 的 20 步序列。模型不能在预测第 k 步前访问该步或更远未来的目标像素与有效掩码。

## 3.2 总体流程

### Figure 1：ObsWorld 总体方法

~~~mermaid
flowchart LR
    subgraph P["Stage1 / Stage1.5：仅预训练阶段"]
        S1["Sentinel-1<br/>雷达观测"] --> EP["多模态 ViT 编码器"]
        S2["Sentinel-2<br/>多光谱观测"] --> EP
        PHI["真实 φ：太阳角、轨道、平台"] --> PEP["φ 编码器"]
        PEP -.->|FiLM 调制| EP
        EP --> SPP["状态投影器 + 重建桥"]
        SPP --> RDP["预训练重建解码器"]
        PEP -.->|真实 φ| RDP
    end

    subgraph I2["Stage2：EarthNet 历史初始化"]
        X["50 天 EarthNet RGBN 历史"] --> BA["4→12 波段适配器"]
        BA --> E2["迁移的视觉编码器"]
        NPHI["固定 neutral φ<br/>非真实逐帧元数据"] --> PE2["迁移并冻结的 φ 编码器"]
        PE2 -.->|中性条件嵌入| E2
        E2 --> SP2["迁移的状态投影器"]
        SP2 --> AGG["覆盖率感知时间聚合"]
        AGG --> Z0["z0：初始预测状态"]
    end

    EP -.->|迁移权重| E2
    PEP -.->|迁移权重| PE2
    SPP -.->|迁移权重| SP2

    subgraph R["Stage2-R：共享五日受控递推"]
        D1["D：降水、温度、VPD、辐射"] --> F["F：共享五日状态转移"]
        C1["C：季节/日历"] --> F
        G1["G：地理/地形"] --> F
        DT["Δt：时间间隔"] --> F
        Z0 --> F
        F --> ZK["z1 ... z20"]
        ZK -->|上一状态继续送入同一 F| F
    end

    subgraph O["Stage2 观测空间验证"]
        ZK --> DEC["O：新的 EarthNet 观测解码器<br/>不接收真实 φ"]
        DEC --> YH["x̂1 ... x̂20<br/>未来 RGBN / NDVI"]
        YH --> LOSS["有效像素预测损失<br/>与真实未来评价"]
    end

    subgraph UO["条件扩展：Stage2-U，当前未进入主结果"]
        OBS["中途部分有效新观测"] -.-> RES["观测－预测特征残差"]
        RES -.-> UP["U：质量/陈旧度门控更新"]
        UP -.-> ZK
    end

    classDef optional fill:#f5f5f5,stroke:#999,stroke-dasharray:5 5,color:#666;
    class OBS,RES,UP optional;
~~~

**图 1 的逻辑意义。** 左上明确区分预训练与 Stage2：真实 φ 只在 Stage1.5 预训练时双端使用，迁移到 EarthNet 后使用固定 neutral φ，不能声称 Stage2 获得真实采集元数据。中间回答“世界怎样向前走”：同一个 F 在每个五日窗口重复使用。右侧回答“状态怎样被检验”：新的 EarthNet 解码器不接收真实 φ，每一步都解码回 RGBN/NDVI。灰色虚线 U 是条件扩展；当前 no-U 投稿版本应移到附录，或明确标注“未纳入当前结果”。

## 3.3 Stage1 与 Stage1.5：φ 条件化预训练及中性条件迁移

Stage1 在 SSL4EO-S12 v1.1 上学习 S1/S2 通用遥感表征。Stage1.5 从 Stage1 继续训练，配置为 ViT-S 规模：embed dimension 384、12 层、6 个注意力头。真实 φ 经过独立编码器，从第 8 层起通过 FiLM 调制预训练视觉编码器，并进入 Stage1.5 重建解码器。

Stage1.5 还包含：

- SpatialStateProjector（空间状态投影器），把视觉 token 映射到 256 维预测状态；
- StateReconstructionBridge（状态重建桥），保证状态仍可用于重建；
- 七天内 S1/S2 配对的 VICReg 对齐；
- cross-covariance nuisance penalty（交叉协方差干扰惩罚）；
- teacher anchor（教师锚定）。

迁移到 EarthNet Stage2 时，只加载 encoder、phi_encoder 与 state projector 等权重。EarthNet 没有兼容的逐帧采集元数据，所以代码对所有历史帧构造固定 neutral φ，并使用新的 EarthNet observation decoder。因此其主张只能是“以中性条件迁移的 φ 条件化状态预训练”，不是“Stage2 实时采集条件感知”，也不是“完全去除了所有 φ 信息”。

形式化为：

$$
z_0=I_\theta(x_{1:T_c};\bar\phi),\qquad \bar\phi\text{ 为固定中性条件}.
$$

## 3.4 physical4 外部驱动

每个五日窗口的驱动为：

$$
D_k=[P_k,T_k,VPD_k,R_k],
$$

其中：

- P：五日降水总量；
- T：五日平均温度；
- VPD：五日平均饱和水汽压亏缺；
- R：五日太阳辐射总量。

日历 C、地理/地形 G 与时间间隔 Δt 作为额外条件。选择 physical4 的原因是物理意义清楚、维度紧凑且已经预先固定；不是因为当前结果已经证明它优于 full24。

## 3.5 Direct-P4：严格配对的非递推对照

Direct-P4 从同一个 z0 出发，结合到目标时距为止的驱动路径，分别预测各个目标状态。它不把前一步预测状态继续送到下一步。

它不是“弱基线”或“工程地基”，而是 matched non-recursive control（严格配对非递推对照）。它与 Rollout-P4 共享：

- Stage1.5 checkpoint；
- physical4、C、G、Δt；
- train/val manifest；
- 解码器；
- 损失；
- H200、batch、epoch 和优化器；
- checkpoint 选择规则。

这使 Direct 与 Rollout 的差异尽量只剩“是否递推共享短步状态”。

## 3.6 Rollout-P4：共享状态递推

Rollout-P4 使用同一个 F 每次推进五天：

$$
z_{k+1}=F_\theta(z_k,D_k,C_k,G,\Delta t_k), \qquad k=0,\ldots,19.
$$

每一步的输出状态继续作为下一步输入，训练和评价时都不使用真实未来状态，构成 open-loop rollout（开放循环递推）。

这里需要区分两个概念：

- 状态递推 20 步：F 的内部过程完整走过 20 个未来窗口；
- horizons_per_sample（每样本监督时距数）：为了显存和计算，训练时可能只解码并监督部分时距，但不能跳过中间状态推进。

正式 Rollout 配置必须记录其时距采样与 curriculum（递推课程），并与 8,800 update / 200 epoch 预算对齐。

## 3.7 观测解码

每个状态被解码为未来 RGBN 均值：

$$
\hat x_{k+1}=O_\theta(z_{k+1}).
$$

需要严格区分两个解码器：Stage1.5 的预训练重建解码器接收真实 φ，用于学习采集条件化表征；Stage2 的 EarthNetObservationDecoder 是新建模块，只从预测状态生成 RGBN，并不接收真实 φ。前者是预训练机制，后者才是本文主预测路径。

## 3.8 训练目标

当前 Direct-P4 正式配置使用：

- valid-mask Huber observation loss（有效像素 Huber 观测损失），权重 1.0；
- NDVI L1 loss，权重 0.5；
- latent、delta 与 smoothness 项权重为 0。

可写为：

$$
\mathcal L =
\frac{\sum_{k,p,c}m_{k,p,c}\,
\operatorname{Huber}(\hat x_{k,p,c},x_{k,p,c})}
{\sum_{k,p,c}m_{k,p,c}+\epsilon}
+
0.5\,
\frac{\sum_{k,p}m^{NDVI}_{k,p}
\left|\widehat{NDVI}_{k,p}-NDVI_{k,p}\right|}
{\sum_{k,p}m^{NDVI}_{k,p}+\epsilon}.
$$

目标有效像素掩码只能用于监督和评价，不能在预测前泄漏未来可见性。

## 3.9 条件扩展 U，不属于当前主方法结果

如果主干冻结后实现 U，则先预测，再更新：

$$
z_{k+1}^{-}=F(z_k,D_k,C_k,G,\Delta t_k),
$$

$$
r_{k+1}=E(x_{k+1}^{obs})-E(\hat x_{k+1}),
$$

$$
z_{k+1}^{+}=U_\psi(z_{k+1}^{-},r_{k+1},q_{k+1},a_{k+1}).
$$

q 是有效可见质量，a 是观测陈旧度。当 q=0 时要求 z 的后验严格等于先验。U 与 uncertainty（不确定性）不同：前者吸收新证据，后者描述对未来有多没把握。

---

# 4. 实验设置

## 4.1 数据与切分

当前主实验只使用 EarthNet2021 预测数据。内部服务器路径中的 earthnet2021x 不改变论文数据名。

必须记录：

- train_dev.json；
- val_dev.json；
- iid.json；
- ood.json；
- extreme.json 与 seasonal.json 只作可选压力测试；
- 每个 manifest 的 SHA256；
- physical4 统计文件及其训练清单哈希；
- 不允许 test 参与 driver、checkpoint、loss 或 schedule 选择。

## 4.2 输入与目标

| 项目 | 设置 |
|---|---|
| 历史上下文 | 50 天 |
| 未来目标 | 100 天 |
| 状态步长 | 5 天 |
| 未来状态数 | 20 |
| 图像目标 | RGBN |
| 植被派生量 | NDVI |
| 主驱动 | physical4 |
| 主开发集 | val_dev |
| 正式测试 | IID / OOD |

## 4.3 正式训练预算

当前 Direct-P4 正式配置：

| 项目 | 设置 |
|---|---|
| GPU | 8 × NVIDIA H200 |
| 每 GPU batch | 64 |
| global batch | 512 |
| optimizer updates | 8,800 |
| 等价训练量 | 200 EarthNet train epochs |
| 每 rank workers | 8 |
| Stage1.5 checkpoint | step 60,000 |
| checkpoint milestones | epoch 100 / 150 / 200 |
| 数值精度 | BF16 训练，TF32 矩阵乘，FP32 评价 |

Rollout-P4 必须使用等价预算；正式启动前只允许进行短 pilot 检查显存、吞吐、梯度与完整 20 步递推。

## 4.4 Checkpoint 选择

只在完整 val_dev 上从预先规定候选中选择。候选集合和主选择指标必须在查看 IID/OOD 前固定。

建议：

1. 候选只取 best monitor、epoch100、epoch150、epoch200；
2. 主选择指标使用冻结 evaluator 的 val 主指标；
3. 平局时优先较早或较简单 checkpoint，不根据 test 打破平局；
4. 写 selected_checkpoint.json，记录所有候选、指标和选择理由；
5. 选中后才允许导出 IID/OOD 预测。

## 4.5 评价指标

为了兼顾植被动态与 EarthNet 历史协议，评估分两层，但主表不能把不同 evaluator 混成一个无说明分数。

### 主文指标

- R²：预测与真实植被变化的相关解释能力，越高越好；
- RMSE：均方根误差，越低越好；
- NSE：相对真实时间变化的效率，越高越好；
- 绝对 bias：系统偏差绝对值，越低越好；
- Outperformance：在多个指标上超过气候态的 minicube 比例，越高越好；
- RMSE25：前 25 天误差。

### 补充指标

- ENS；
- MAD；
- OLS；
- EMD；
- SSIM；
- RGBN-MAE 与 NDVI-MAE；
- 参数量、显存、吞吐和延迟。

> [!warning] evaluator 决策门
> 如果 legacy EarthNetScore 与当前内存指标的 mask、target 或目录评分不一致，先完成 parity test（等价性测试），再报告 ENS。任何未通过来源核验的历史 iid_best.json 都不能进入主表。

## 4.6 报告精度与统计

| 指标 | 显示精度 |
|---|---|
| R² / NSE / RMSE / 绝对 bias | 3 位小数 |
| ENS / MAD / OLS / EMD / SSIM | 4 位小数 |
| MAE / NDVI-MAE | 4 位小数 |
| Outperformance | 1 位小数百分点 |
| Params / FLOPs | 0.1M / 0.1G |
| latency | 0.1 ms |

最终可训练方法报告 3 seed mean±std。关键对比 Rollout-P4 vs Direct-P4 使用相同样本上的 tile-cluster paired bootstrap 95% CI。若外部方法只重跑一个 seed，使用 † 标记，不能伪装成三 seed。

## 4.7 对比方法

### 主表必需

1. Persistence；
2. Climatology；
3. Contextformer；
4. PredRNN 或 SimVP 至少一个；
5. Direct-P4；
6. Rollout-P4。

### 相关工作必需、主表条件性

- Earthformer：成本较高，可单 seed；
- VegeDiff：若能严格适配相同输入输出与 evaluator；
- EO-WM：当前模型/协议若无法完整复现，放相关工作与协议说明；
- VegSim：输出以 NDVI 点预测/概率预测为主，不可直接混入 RGBN 主表。

### 暂不需要

- 大模型 + ours：会把方法收益与模型规模混在一起，当前不是正文必需；
- 额外下游分类：不能直接证明长期预测状态好；
- 新数据集：当前时限下只会分散主实验。

---

# 5. 主结果与分析

> [!important] 本节填数纪律
> 所有【待填】必须来自冻结后的结果 bundle；不得从训练日志截图、不同 evaluator 或不同 manifest 手抄数字。

## 5.1 主结果

### Table 1：EarthNet2021 同协议长期预测主结果

| 方法 | 类型 | IID R² ↑ | IID RMSE ↓ | IID Outperf ↑ | OOD R² ↑ | OOD RMSE ↓ | ENS ↑ | Params |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| Persistence | 非学习 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 0 |
| Climatology | 非学习 | 【待跑】 | 【待跑】 | — | 【待跑】 | 【待跑】 | 【待跑】 | 0 |
| Contextformer | 强基线 | 【同协议待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待填】 |
| PredRNN / SimVP | 强基线 | 【同协议待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 | 【待填】 |
| Direct-P4 | 配对非递推对照 | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| **ObsWorld Rollout-P4** | **共享递推主模型** | **【待填】** | **【待填】** | **【待填】** | **【待填】** | **【待填】** | **【待填】** | **【待填】** |

表注：

- 所有模型必须使用相同 train/val/test manifest、目标 mask 与 evaluator；
- mean±std 来自 3 seed；单 seed 使用 †；
- 粗体只能标同一列最佳，不能预先加粗待填值；
- 如果 ENS evaluator parity 尚未通过，删除 ENS 列而不是填未经核验的值。

**Table 1 的逻辑意义。** Persistence 和 Climatology 判断模型是否获得超过简单时间规律的技能；Contextformer 与 PredRNN/SimVP 判断是否超过强预测器；Direct-P4 与 Rollout-P4 是文章最关键的因果式结构对照，判断共享短步状态递推本身是否有价值。只有最后一对公平，世界模型主张才站得住。

### 结果文字模板

请按真实结果选择一种，不要把三种混写。

**情形 A：Rollout 明显更好**

> 在相同初始化、physical4 驱动和训练预算下，Rollout-P4 相对 Direct-P4 在 IID 的【指标】提高【待填】，在 OOD 提高【待填】；配对瓦片 bootstrap 的 95% CI 为【待填】，不跨越零。增益主要出现在【待填】天以后，说明共享短步状态推进改善了长期预测。

**情形 B：总体相当，长时距更稳**

> Rollout-P4 与 Direct-P4 在总体主指标上差异为【待填】，处于预先规定的非劣范围；但在【待填】天以后或 OOD 上，Rollout-P4 的误差增长更慢【待填】。这表明共享递推在不牺牲总体精度的前提下提供了统一状态过程。

**情形 C：Rollout 更差**

> Rollout-P4 相对 Direct-P4 在【指标】上下降【待填】，表明当前共享转移仍受误差累积影响。该结果不足以支持“递推优于非递推”的主张；正文应收缩为结构检验，且在修复前不启动 U 或 Partition 大训练。

## 5.2 时距行为

### Figure 2：5–100 天预测误差随时距变化

~~~text
【待生成正式折线图】

横轴：5, 10, 15, ..., 100 天
左纵轴：NDVI-RMSE 或 R²
右纵轴（可选）：RGBN-MAE
曲线：Persistence、Contextformer、Direct-P4、Rollout-P4
阴影：3 seed 或 tile bootstrap 95% CI
竖向标记：25 天、50 天、100 天
~~~

**Figure 2 的逻辑意义。** Table 1 只给总分，不能说明模型何时开始失败。Figure 2 用完整时距曲线判断 Rollout 是否因递推迅速崩溃、是否只在短期有效、以及相对 Direct 的优势是否真正出现在长期。本文的“长期预测状态”主张主要依赖这张图，而不是只依赖一个平均分。

### 图旁分析模板

- 0–25 天：【待填短期现象】；
- 25–50 天：【待填中期现象】；
- 50–100 天：【待填长期现象】；
- OOD 与 IID 曲线差异：【待填】；
- 是否出现过度平滑、向气候态收缩或误差爆炸：【待填】。

## 5.3 驱动使用与分布稳健性

### Table 2：真实驱动、错误驱动与 OOD 的诊断结果（当前 no-U 版）

| 模型/干预 | IID R² ↑ | IID RMSE ↓ | OOD R² ↑ | OOD RMSE ↓ | 相对真实 D 的变化 | 解释 |
|---|---:|---:|---:|---:|---:|---|
| Rollout-P4 + true D | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 0 | 正常主模型 |
| Rollout-P4 + no D | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 是否真正依赖天气 |
| Rollout-P4 + shuffled D | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 错地点/错样本驱动 |
| Rollout-P4 + time-shifted D | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 时间对齐是否重要 |
| Direct-P4 + true D | 【待填】 | 【待填】 | 【待填】 | 【待填】 | — | 非递推参照 |

**Table 2 的逻辑意义。** 如果模型在打乱或错位天气后几乎不变，那么“受外部驱动的世界模型”只是输入形式，不是学到的行为。真实 D 应优于 no-D 与错误 D；但不能要求错误 D 必然让所有指标大幅下降，因为季节和历史状态本身也有预测力。

### 安全解释规则

- true D 明显优于 no-D 和 shifted-D：可写“模型利用了时间对齐驱动”；
- true D 只略优：写“驱动提供有限但一致收益”；
- 完全无差：删除“驱动响应”强主张，检查模型是否忽略 D；
- shuffled D 反而更好：优先排查数据对齐、归一化或混杂，不能挑另一个指标掩盖。

## 5.4 定性结果与失败案例

### Figure 3：代表性长期轨迹、空间误差与失败模式

~~~text
【待生成正式复合图】

行：
A. 真实未来 RGB / NDVI
B. Contextformer 或强基线
C. Direct-P4
D. Rollout-P4
E. 绝对误差图

列：
day 5 / day 25 / day 50 / day 75 / day 100

右侧小图：
同一 minicube 的真实 NDVI、Direct、Rollout 时间轨迹

至少展示：
1 个成功案例 + 1 个失败案例
并标注有效像素比例、地表类型、季节和 OOD/IID
~~~

**Figure 3 的逻辑意义。** 这张图不是“挑最好看的图”。成功案例说明改进的空间结构和时间行为；失败案例揭示长期平滑、云边界、收割/火灾等未建模事件或驱动失配。定性结果必须由固定规则选取，例如按 Rollout-Direct 差异的中位数、上四分位和下四分位，而不是人工挑图。

---

# 6. 消融与机制分析

## 6.1 Stage1.5、递推与驱动表示

### Table 3：关键组件的紧凑消融

| 编号 | 初始化 | 预测方式 | 驱动 | val R² ↑ | IID R² ↑ | OOD R² ↑ | 长时距 RMSE ↓ |
|---|---|---|---|---:|---:|---:|---:|
| A | Stage1.5 | Direct | physical4 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| B | Stage1 | Rollout | physical4 | 【待跑】 | 【待跑】 | 【待跑】 | 【待跑】 |
| C | Stage1.5 | Rollout | physical4 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| D（可选附录） | Stage1.5 | Rollout | full24 | 【可延后】 | — | — | 【可延后】 |

比较关系：

- C 对 A：共享递推相对 Direct；
- C 对 B：Stage1.5 初始化的预测价值；
- C 对 D：只有做了 full24 才能讨论 physical4 输入选择。

**Table 3 的逻辑意义。** 这张表防止把所有收益都归给“ObsWorld”这个整体名字。它分别问：状态初始化是否有用、递推是否有用、额外驱动维度是否有用。D 是可选行，缺失不影响 A–C 的主闭环。

## 6.2 φ 泄漏如何写

正文只需要一句边界：

> Stage1.5 的交叉协方差与双端 φ 条件鼓励状态把已知成像因素交给条件分支解释，但我们不把它解释为对任意非线性采集信息的完全独立性。

若修正版 probe 完成，把结果放附录，不占正文主表。旧 67%–71% 结果不得使用。

## 6.3 Partition 是否需要

当前正文不放 Partition。它只比较相同十天控制路径的一次推进与 5+5 分段推进，最多支持近似分段一致性，不证明严格半群、因果或物理正确性。

只有以下条件同时满足才进入附录：

1. Rollout-P4 已通过；
2. Partition gap 明显下降；
3. factual prediction（事实预测）不退化；
4. 不增加另一套正文主线。

---

# 7. 讨论

## 7.1 为什么 Direct 强不是坏事

Direct 是严格对照，不是被故意做弱的基线。Direct 越强，Rollout 的任何优势越可信。若 Direct 最终最好，正确结论是当前任务或训练预算下直接多时距预测更合适，而不是贬低 Direct 或隐藏结果。

## 7.2 为什么当前仍可称为世界模型

本文不依靠名称，而依靠可检验结构：

1. 从历史观测形成内部预测状态；
2. 在给定外生驱动下推进状态；
3. 用同一转移反复进行开放循环；
4. 将状态解码回真实观测空间进行逐时距验证。

这支持“预测状态世界模型”。它不支持完整地球模拟器或数字孪生。

## 7.3 与 EO-WM、VegSim 的关系

EO-WM 强调概率视频扩散与物理信息气象条件，VegSim 强调情景气象下的 NDVI 概率模拟。ObsWorld 当前应强调：

- S1/S2 跨模态预训练到预测状态的连接；
- Stage1.5 真实 φ 条件化预训练及其以中性条件迁移到 Stage2 的明确边界；
- Direct 与 Rollout 的严格配对结构检验；
- 完整 RGBN 观测空间与 NDVI 双重验证。

如果没有 U，本文的 novelty（新颖性）压力仍然较大，因此主实验必须非常严谨，不能只报告一个比 baseline 高的总分。

## 7.4 下游任务与“大模型 + ours”

它们暂不放正文，理由不是“永远没用”，而是与核心主张距离较远：

- EuroSAT 分类说明表征可迁移，但不能证明长期地表递推；
- 大模型 + ours 同时改变容量和方法，难判断收益来源；
- AAAI-27 主文只有 7 页，关键证据必须优先给 Direct、Rollout、时距与 OOD。

若主实验完整后仍有预算，下游与规模实验可放补充材料。

## 7.5 限制

至少诚实写出：

1. 未来气象驱动来自已记录轨迹，不代表部署时完美天气预报；
2. 当前只在 EarthNet2021 欧洲区域协议上验证；
3. 观测稀疏、云和人类活动造成不可观测变化；
4. physical4 是预先选择的紧凑驱动，不是完备物理状态；
5. 当前模型是确定性预测；不确定性建模不是本文主贡献；
6. no-U 版本不能声称已能吸收中途新观测；
7. Stage1.5 正则不保证完全非线性去耦；
8. 预测状态不等于唯一真实物理状态。

---

# 8. 结论

本文提出 ObsWorld，一个面向长期地球观测预测的共享转移预测状态世界模型。模型把经过跨模态与真实 φ 条件训练的预训练权重以固定中性条件迁移到 EarthNet，在紧凑 physical4 外部驱动下复用共享五日转移，并将每一步状态解码到可验证的多光谱观测空间。通过与严格配对 Direct-P4、强预测基线、时距曲线、OOD 和驱动扰动比较，我们发现【待填真实结论】。这些结果说明【待按决策门填写，不得超出结果】。未来工作将研究概率预测、部署时天气不确定性，以及在新观测到来时进行安全状态更新。

---

# 9. 结果部分的 3 图 3 表闭环检查

| 论文主张 | 主证据 | 辅助证据 | 若失败怎样收缩 |
|---|---|---|---|
| ObsWorld 有预测技能 | Table 1 | Figure 3 | 只保留方法，不写强性能主张 |
| 共享递推状态有用 | Direct vs Rollout 的 Table 1 | Figure 2 | 删除“递推更优”，先修 Rollout |
| 模型真实利用驱动 | Table 2 | OOD 结果 | 改写为条件输入，不写响应性 |
| Stage1.5 有预测价值 | Table 3 B vs C | 收敛曲线 | 降为训练细节 |
| 改进发生在长期 | Figure 2 | Table 1 OOD | 改写为总体预测，不强调长期 |
| 改进具有可解释空间行为 | Figure 3 | 分地表类型附录 | 诚实报告失败模式 |
| U 可校正状态（条件） | U 版 Table 2 | U 版 Figure 3 | 不升级标题，U 放未来工作 |

这就是“证据与决策门表”：不是预先希望每格都赢，而是规定每句论文话由哪张图表批准。

---

# 10. AAAI-27 篇幅安排

AAAI-27 主技术轨主文限 7 页，最多 9 页且第 8–9 页仅参考文献。建议正文布局：

| 部分 | 目标页数 |
|---|---:|
| 摘要与引言 | 1.2 |
| 相关工作 | 0.6 |
| 方法 + Figure 1 | 2.0 |
| 实验设置 | 0.8 |
| 结果：3 图 3 表 | 2.1 |
| 讨论、限制与结论 | 0.3 |
| 合计 | 7.0 |

关键结果不能只放补充材料，因为官方说明审稿人不必阅读 supplement（补充材料）。完整配置、更多结果、U 科普、φ probe 与 Partition 放附录。

---

# 附录 A：U（观测更新）科普与可执行设计

## A.1 U 不是不确定性

| 概念 | 问题 | 常见输出 |
|---|---|---|
| Uncertainty（不确定性） | 未来有多不确定 | 方差、分布、多条样本 |
| Observation correction（观测校正） | 新证据到了，状态怎样改 | 更新后的状态与后续预测 |

例子：模型在 day 0 预测未来 100 天；day 25 卫星拍到一张局部清晰新图。

- 不确定性模块会告诉我们原来预测有多不确定；
- U 会把 day 25 新图和 day 25 预测之间的差异吸收到状态里，再预测 day 30–100。

## A.2 当前完成度

已实现：

- ObservationCorrectionCell；
- predict-before-update（先预测再更新）契约；
- q=0 exact identity（无有效观测严格不变）；
- visibility weighting（可见性加权）；
- staleness update（观测年龄更新）；
- 独立单元测试。

尚未实现：

- dataset reveal 字段；
- 与真实观测 encoder/projector 的连接；
- obsworld_factory 注册；
- Stage2 trainer 混合日程；
- 正式 day25/day50 evaluator；
- Restart、Blend、VanillaFilter 与 online recurrent 强基线；
- 端到端 checkpoint 与结果。

所以 U 不是当前 Stage2 正式训练的一部分。

## A.3 推荐训练

1. 冻结通过主实验的 Stage2-R；
2. 50% no-reveal，50% exactly-one-reveal；
3. reveal 在未来 step 2–15；
4. 先产生该步预测；
5. 只用有效区域编码真实观测；
6. 计算观测特征减预测特征；
7. 用 q 与 staleness 门控状态修正；
8. 只对 reveal 后未来施加主要收益损失；
9. q=0 时强制 exact no-op；
10. 单 seed val 胜过强基线后，再考虑联合微调和多 seed。

## A.4 U 版 Table 2

| 更新方法 | day25 后 RMSE ↓ | day25 Gain-AUC ↑ | day50 后 RMSE ↓ | day50 Gain-AUC ↑ | no-reveal 退化 |
|---|---:|---:|---:|---:|---:|
| No-update | 【待填】 | 0 | 【待填】 | 0 | 0 |
| Re-encode / restart | 【待填】 | 【待填】 | 【待填】 | 【待填】 | — |
| Naive blend | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| VanillaFilter / GRU | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| ObsWorld-U | 【待填】 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |

通过条件不是“相对自己 no-update 有一点改善”，而是优于容量匹配在线更新器，并在无观测路径不退化。

---

# 附录 B：Stage1.5、φ 与非线性泄漏

## B.1 为什么当前训练策略合理

在 Stage1.5 预训练中，真实 φ 同时进入 encoder 与重建 decoder，不是“把干扰偷偷加回去”，而是角色分工：

- encoder 知道采集条件，能解释当前图像为何这样呈现；
- state bottleneck（状态瓶颈）保留对跨模态和未来有用的信息；
- decoder 知道采集条件，能从状态生成条件正确的观测；
- alignment、cross-covariance 与 teacher anchor 约束状态不要任意漂移。

这是一种仅发生在 Stage1.5 的 acquisition-conditioned factorization（采集条件化分解），不是强制完全不变。EarthNet Stage2 使用固定 neutral φ 与新的无 φ 解码器，不能把预训练机制写成部署时的真实采集条件输入。

## B.2 为什么旧 67%–71% 不可用

旧脚本存在五个关键问题：

1. Stage1.5 提取时既没有传真实 φ，也没有走 Stage2 的固定 neutral φ 路径；
2. Stage1 使用随机 state projector；
3. 错误丢弃第一个 patch token；
4. probe 训练和测试没有严格空间隔离；
5. 没有 metadata-only 对照。

因此不能从旧数值得出当前双端 φ 模型仍有 67%–71% 非线性泄漏。

## B.3 修正版评估

| 输入 | probe | 目的 |
|---|---|---|
| metadata only | ridge / MLP | 元数据本身能预测多少 |
| state only | ridge / MLP | 状态能预测多少 |
| metadata + state | ridge / MLP | 状态在元数据之外增加多少 |
| metadata + shuffled state | ridge / MLP | 排除容量和偶然相关 |

切分按地理 tile，并尽量做时间隔离；报告 5 seed、balanced accuracy/AUROC、sun elevation MAE 与增量 ΔR²/ΔAUROC。

论文最重要的 Stage1.5 证据仍然是 Stage1 vs Stage1.5 的 Rollout-P4 预测消融，而不是单独追求低 probe 精度。

---

# 附录 C：Partition（分段一致性）科普

## C.1 它是什么

同一十天控制路径有两种走法：

$$
z_{10}^{direct}=F(z_0,D_{0:10},C_{0:10},G,\Delta t_{0:10}),
$$

$$
z_5=F(z_0,D_{0:5},C_{0:5},G,\Delta t_{0:5}),
$$

$$
z_{10}^{compose}=F(z_5,D_{5:10},C_{5:10},G,\Delta t_{5:10}).
$$

比较两个终点状态、观测和 NDVI 是否接近。

## C.2 它能证明什么

只能说明模型对这一条相同控制路径的“10 天一次走完”和“5+5 分段走完”近似一致。

## C.3 它不能证明什么

- 严格半群；
- 任意步长组合；
- 真实因果或物理定律；
- 长期预测一定更好。

因此它是可删除的附录实验，不阻塞 Direct-P4 和 Rollout-P4。

---

# 附录 D：已发表参考数值，不是本项目主结果

## D.1 Contextformer CVPR 2024 Table 2

以下使用其论文协议，只建立量级；在我们完成同协议重跑前不能与本项目待填值放在同一“公平主表”中。

| 方法 | R² ↑ | RMSE ↓ | NSE ↑ | 绝对 bias ↓ | Outperf ↑ | RMSE25 ↓ | Params |
|---|---:|---:|---:|---:|---:|---:|---:|
| Persistence | 0.00 | 0.23 | -1.28 | 0.17 | 21.8% | 0.09 | 0 |
| Previous year | 0.56 | 0.20 | -0.40 | 0.14 | 19.3% | 0.18 | 0 |
| Climatology | 0.58 | 0.18 | -0.34 | 0.13 | — | 0.16 | 0 |
| Earthformer† | 0.52 | 0.16 | -0.13 | 0.10 | 56.5% | 0.09 | 60.6M |
| PredRNN | 0.62±0.00 | 0.15±0.00 | 0.03±0.00 | 0.10±0.00 | 64.7±1.2% | 0.10±0.00 | 1.4M |
| SimVP | 0.60±0.00 | 0.15±0.00 | 0.03±0.01 | 0.09±0.00 | 64.1±1.0% | 0.10±0.00 | 6.6M |
| Contextformer | 0.62±0.00 | 0.14±0.00 | 0.09±0.01 | 0.09±0.00 | 66.8±0.3% | 0.08±0.00 | 6.1M |

† 单 seed。

## D.2 如何使用这些数值

可以说：

> Contextformer 在其公开协议上报告 R² 0.62、RMSE 0.14 和 Outperformance 66.8%，构成重要参考目标。

不能说：

> 我们的【另一协议数值】已经超过 Contextformer，因此达到 SOTA。

除非同 manifest、同 mask、同 evaluator 重跑。

---

# 附录 E：当前已有 Stage1 历史结果

| checkpoint | 训练量 | EuroSAT linear probe OA |
|---|---:|---:|
| Stage1 50k | 约 100 epoch | 69.57% |
| Stage1 95k | 约 200 epoch | 76.15% |
| SSL4EO 论文 MAE ViT-S/16 参考 | 论文设置 | 94.1% |

已确认事实：

- 95k 相对 50k 提升 6.58 个百分点；
- 我们的 95k 结果仍低于已发表 MAE ViT-S/16 参考；
- 模型规模、双模态目标和评估设置存在差异；
- 该表只能说明 Stage1 历史表征背景，不能证明 Stage1.5 或 Stage2 的预测贡献。

---

# 附录 F：full24 是否需要

full24 只回答输入表示问题：

- physical4：4 个紧凑物理聚合量；
- full24：8 个日变量的 mean/min/max，共 24 维。

当前策略：

1. 第一套完整主实验全部 physical4；
2. 不因等待 full24 推迟 Rollout-P4；
3. 主干冻结后若仍有预算，只在 val_dev 单 seed 做附录对照；
4. 若不做，不声称 physical4 更优；
5. 若做，除 driver encoder 输入外其余设置严格相同。

---

# 附录 G：复现与结果冻结清单

每一行正式结果必须能追溯到：

- 方法名与 seed；
- Git commit；
- config 文件与 SHA256；
- Stage1.5 checkpoint 与 SHA256；
- Stage2 selected checkpoint 与 SHA256；
- train/val/test manifest 与 SHA256；
- physical4 stats 与 SHA256；
- evaluator 版本与 mask 定义；
- prediction manifest；
- score provenance；
- GPU、batch、updates、epoch；
- 参数量、峰值显存、吞吐；
- 是否单 seed；
- 是否来自外部论文而非本地重跑。

发布前检查：

- [ ] test 未用于选 checkpoint 或调参；
- [ ] Direct 与 Rollout 预算公平；
- [ ] 3 seed 或明确标 †；
- [ ] 所有【待填】已替换；
- [ ] 所有最佳值加粗基于真实数字；
- [ ] ENS parity 已通过，或已删除 ENS；
- [ ] U 未通过时标题/摘要没有 correction；
- [ ] full24 未跑时没有比较结论；
- [ ] 旧 φ probe 数字没有进入论文；
- [ ] 限制部分没有删除负面边界；
- [ ] AAAI reproducibility checklist 已填写。

---

# 附录 H：术语表

| 英文 | 中文解释 |
|---|---|
| acquisition condition, φ | 采集条件：太阳角、轨道、卫星平台等 |
| acquisition-conditioned pretraining | 预训练时使用真实采集条件；当前 Stage2 只使用中性条件迁移权重 |
| predictive state | 为未来预测维护的内部状态 |
| world model | 维护状态、推进状态并产生可验证观测的模型 |
| direct prediction | 从同一初始状态直接预测各未来时距 |
| rollout | 把上一步预测状态继续向前推进 |
| open-loop | 未来途中不喂真实状态 |
| controlled transition | 在外部驱动条件下的状态转移 |
| physical4 | 降水、温度、VPD、辐射四维驱动 |
| full24 | 八个日变量的均值/最小/最大，共 24 维 |
| IID | 与训练分布相近 |
| OOD | 与训练分布不同 |
| observation correction, U | 新观测到来后更新内部状态 |
| uncertainty | 模型对未来有多没把握 |
| reveal | 在指定未来时点向模型提供真实观测 |
| staleness | 距上次有效观测的时间 |
| matched control | 除目标变量外其余设置相同的对照 |
| ablation | 去掉或替换组件以判断其贡献 |
| non-inferiority | 在预先规定范围内不比对照差 |
| paired bootstrap | 在同一样本上重复抽样估计差异置信区间 |
| provenance | 数据、代码、配置、模型与结果来源记录 |
| SOTA | state of the art，当前最佳；必须同协议才可比较 |

---

# 附录 I：引用与事实核查来源

- [AAAI-27 Main Technical Track Call](https://aaai.org/conference/aaai/aaai-27/main-technical-track-call/)
- [AAAI-27 Conference and deadlines](https://aaai.org/conference/aaai/aaai-27/)
- [Contextformer / Multi-modal Learning for Geospatial Vegetation Forecasting, CVPR 2024](https://openaccess.thecvf.com/content/CVPR2024/html/Benson_Multi-modal_Learning_for_Geospatial_Vegetation_Forecasting_CVPR_2024_paper.html)
- [EarthNet2021](https://arxiv.org/abs/2104.10066)
- [VegeDiff](https://arxiv.org/abs/2407.12592)
- [EO-WM](https://arxiv.org/abs/2606.27277)
- [VegSim](https://arxiv.org/abs/2606.21961)
- [SSL4EO-S12](https://arxiv.org/abs/2211.07044)
- [Locatello et al., 2019：无监督去耦的可识别性边界](https://proceedings.mlr.press/v97/locatello19a.html)

---

# 最后冻结结论

> **从现在到第一套主结果形成，只推进 physical4 的 Direct-P4 与 Rollout-P4。Direct 是严格配对对照，Rollout 是当前主模型；完成 seed 42 的 val 选择、冻结和 IID/OOD 评估后，才补多 seed 和强基线。full24 与 Partition 不阻塞；Stage1.5 用预测消融证明；U 属于主干冻结后的 Stage2-U 条件升级，不是不确定性，也尚未进入正式训练。**
