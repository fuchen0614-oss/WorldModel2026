# 工作交接 · 2026-08-14 → 08-15

> **文档性质**：跨两条线的工作台账与待办清单，供训练端接手后**直接在本文件上继续完善**。
> **不修改**已提交的 AAAI 正文，不把计划写成已完成的结果。
> 上游依据：`obsworld/思路整理进展/89_双线推进_...20260813.md`（研究侧冻结优先级）。

---

## 一、08-14 已完成

### 1.1 AAAI-27 补充材料与代码包审查

对 `terrastate/submission/` 下的两个投稿件做了独立复核（不是读 QA 文档，是实际验证）。

**通过的项**

| 检查 | 结果 |
|---|---|
| `supplementary.pdf` | 3 页 / US Letter 612×792pt / 两栏 AAAI-27 |
| 字体 | 16 个全部 embedded，**零 Type 3**（AAAI 硬性要求） |
| PDF 元数据 | 只有 pdfTeX、TeX，**无 Author 字段** |
| `latexmk` 日志 | 无 error、无 undefined reference、无 overfull/underfull box |
| 交叉引用 | "Sections 3 and 4" ✓、Table 2 = Q2 ✓、Table 3 = Q3 ✓、Section 4 的 *Experimental Setup* = 4.1 ✓ |
| `results/*.json` vs 正文 Table 1–3 | **逐项数值完全一致**（0.56935 / 0.15059 / 0.082、0.49732、0.01121、0.01616 [0.00643, 0.02590]、0.6254 / 0.1492、56/84、69/84、0.03592 / 0.08137） |
| 代码包 ZIP | CRC OK、单顶层目录、27 文件、无 symlink、无路径穿越 |
| 匿名扫描 | 姓名 / 邮箱 / `/mnt/` / `/home/` / wandb / git remote / token：**0 真命中** |
| CPU smoke | 干净解压后实机运行 → `CPU_SMOKE_PASS` |
| 代码 vs 附录描述 | `register_buffer("alpha", 1.0)`、`prediction = prior + alpha*state_scale*contribution`（state removal 恰好得 $b_h$）、`_context_only()` 清零未来光学/气象/mask、非递归 `all_prefixes` 直接查询 —— 均与附录 A 一致 |

**表格 caption 位置的裁定**：AAAI-27 AuthorKit `AnonymousSubmission2027.tex:590` 明文要求
"The number and caption for your table must appear *under* (not above) the table"。
PDF 是对的，`SUPPLEMENT_QA.md` 里"caption above"那句是**文档写错**，PDF 无需改动。

### 1.2 审查中发现的待办（按严重度）

| 级别 | 问题 | 位置 |
|---|---|---|
| 中 | 补充材料解释了 validation 的 952→589，但**没解释 OOD-t 的 1,904→1,019**；正文 Table 2 caption 写了 `n=1,019`，读者查不到出处 | `supplementary.tex` Appendix B.2 |
| 低 | Q2 评测脚本**没有 `--manifest`**，直接遍历整个 `--data-root`；`manifests/q2_splits.json` 只有计数、脚本读不了 → Q2 的 split 选择不可复现（Q1/Q3 都有 manifest） | `eval/evaluate_state_load_bearing.py` |
| 低 | `configs/terrastate.yaml` 的 `training.loss.*` 与 `history_frozen_fraction` **从未被代码读取**，训练脚本硬编码 `0.5 * loss_kd` 和 `schedule_weight()`。数值一致，但配置是装饰性的 | `train/train_terrastate.py` |
| 低 | `payload.get("b" + str(4) + "_state_dict", payload)` 这行字符串拼接极不自然，反而引人注意 | `models/terrastate.py:355` |
| 提示 | 已提交正文**0 处**提到 supplement / appendix，审稿人可能根本不会打开 | `submission/main.tex` |

### 1.3 两个代码包 ZIP 的差异（需决策）

`terrastate/submission/aaai27_code_package/` 下有两个 zip：

| | `TerraState_CodeData.zip` | `TerraState_CodeData_FINAL.zip` |
|---|---|---|
| 时间 | 07-30 16:55 | **07-31 17:07** |
| `SHA256SUMS.txt` 收录 | ✅ | ❌ |
| `UPLOAD_GUIDE.md` 指向 | ✅ | ❌ |

`_FINAL` 相对旧版：

- ✅ 删掉了 `"b" + str(4) + "_state_dict"` 混淆写法
- ✅ `cluster_interval` 重写为 numpy 实现，分组前先过滤非有限值
- ✅ 新增 `protocols/q3_weather_response/{protocol,climatology_summary}.json`
- ⚠️ **`configs/terrastate.yaml` 的 `seed: 42` 改成了 `seed: 0`**

最后一条是回退风险：`results/*.json` 里的 bootstrap CI 是用**原实现 + seed 42** 算的，换 RNG 又换 seed 会导致重跑得到不同区间。

> **注意**：AAAI-27 补充材料截止是 2026-07-31 AoE，**已经过去两周**。这一组问题不影响那次投稿，只影响 camera-ready 或后续 TIP 扩展。

### 1.4 仓库重构

`main` 拆成两个互不依赖的项目根目录。

**重构依据（关键量化结论，代码里看不出来）**：对 TerraState（plan-b 线）的 91 个新增 `.py` 做传递依赖闭包（56 个 .py 节点），发现它**只依赖原 main 底座的 5 个文件**：

```
data/earthnet_manifest.py
eval/eval_greenearthnet_official.py
eval/greenearthnet_protocol.py
eval/table1_artifact_contract.py
models/encoders/state_projection.py
```

这 5 个已两边各复制一份，此后独立演化。佐证：plan-a 与 plan-b 的新增文件交集 = **0**；两者修改集合交集 = **0**；三次 `git merge-tree` 干跑全部零冲突。

| | terrastate/ | obsworld/ |
|---|---|---|
| 跟踪文件 | 392 | 700 |
| `思路整理进展/` | 30 篇 | 77 篇 |
| `任务描述相关/` | — | 26 篇 |

100 篇规划文档按「最终归属」硬分；7 篇确实跨两条线的在两边各留一份（`33`、`39`、`45`、`46`、`72`、`73`、`89`）。

**过程中救回的东西**（原本会静默丢失）：

- 6 个未提交的新文档：`86/87/88 TerraState`、`89_双线推进`、`GreenEarthNet同数据集复现差异案例`，以及 `evaluations/officialENS_run1/`
- 继承的 `.gitignore` 本会吞掉 19 个证据文件，含 `terrastate_behavioral_evidence.csv`（图 3 数据源）与 `metrics_en21x.csv`（Stage2 关键结果）。已重写 `.gitignore` 并加例外规则
- `_audit/` 里 652 MB 的 TinyTeX / tectonic 二进制已剔除

**分支**

```
main                        双项目结构（当前工作分支）
archive/plan-a-vits         ObsWorld Plan A / A′（Stage2 延续、S1a、Table B）
archive/plan-b-pvt          TerraState 论文证据链（boundary80 evidence、OOD-t metrics、主表）
archive/plan-b-v2-train     TerraState-V2 训练与运维基建
agent/obsworld-aaai27-planning   陈旧，未动
```

三条 archive 分支只读，不再合并。回滚点在本地 tag `prerestructure/*`（**未推送**，只在 `WorldModel2026/.git`）。

### 1.5 权重上传

8 个 / 1.42 GB 全部上传并校验：

| Release | 资产 | 大小 |
|---|---|---|
| [`weights-terrastate-v1`](https://github.com/fuchen0614-oss/WorldModel2026/releases/tag/weights-terrastate-v1) | 3 | 133.4 MB |
| [`weights-obsworld-v1`](https://github.com/fuchen0614-oss/WorldModel2026/releases/tag/weights-obsworld-v1) | 5 | 1.29 GB |

全部 `state=uploaded` 且字节数吻合；`plan_a_s1a_full24__checkpoint_best.pt` 额外下载回来验过 SHA-256。
对照表与下载配方见两个 `WEIGHTS_INDEX.md`。

---

## 二、08-15 待办

### P0 · 立刻

- [ ] **撤销泄露的 GitHub token**（https://github.com/settings/tokens）。一个 `repo` scope 的 PAT 曾在会话中明文出现，本地副本已 `shred`，但远端凭证仍有效。需要的话重开一个，用 `gh auth login` 或写进 `~/.netrc`（`chmod 600`），不要再贴进对话。

### P0 · 训练端接手

- [ ] **拉取新结构**：`git clone git@github.com:fuchen0614-oss/WorldModel2026.git WorldModel2026v2 && cd WorldModel2026v2 && git checkout main`
      期望 1096 个跟踪文件、约 128 MB；对不上先停下报告。
- [ ] **就位权重**：训练机上本来就有这 8 个文件，优先本地找回（按字节数 `find` 出候选再算 SHA-256，不要全盘哈希）；找不到的再从 Release 拉。逐个校验 SHA-256，只有大小对但哈希不对的记为「同尺寸内容不同」，不要放进去。
- [ ] **搜寻缺失的 14,880-update 最终权重** ← **本次最高价值的单项任务**
      论文口径的 40 epochs / 14,880 optimizer updates 最终权重，其二进制**在任何地方都没有找到**，已恢复的 `boundary80` 不能替代它。它很可能只存在于训练服务器。
      线索：文件名含 `terrastate` / `b4` / `full24` / `14880` / `epoch40` / `final`；训练输出目录里的 `checkpoint_last.pt` / `checkpoint_best.pt`。
      判定（不要整个加载到显存）：
      ```python
      import torch
      d = torch.load(PATH, map_location='cpu', weights_only=False)
      print({k: d[k] for k in ('epoch','optimizer_update','global_step') if k in d})
      ```
      找到就记录**完整路径 + SHA-256 + 字节数 + 内部 epoch/update 字段**，单独报告。
- [ ] **路径迁移**：训练脚本现在带项目前缀，例如 `run_stage1_5_vits_60k.sh` → `obsworld/run_stage1_5_vits_60k.sh`。检查所有 launcher、cron、nohup 命令里的硬编码路径。
- [ ] **不要 commit 权重**。`.gitignore` 已排除 `*.pt` / `*.ckpt`，不要用 `git add -f` 绕过。

### P1 · 代码包决策（影响 camera-ready / TIP）

- [ ] 决定 `_FINAL.zip` 还是旧 `.zip` 作为正式版本。建议**用 `_FINAL` 但把 `seed` 改回 42**，然后重算 SHA、更新 `UPLOAD_GUIDE.md` 与 `SHA256SUMS.txt`。
- [ ] 补上 OOD-t `1,904 → 1,019` paired units 的说明（一个从句即可，见 1.2 表第一行）。
- [ ] 给 `evaluate_state_load_bearing.py` 加 `--manifest`，并把文件列表写进 `q2_splits.json`，让 Q2 的 split 可复现。

### P1 · 研究推进（依据 89 号文档的冻结优先级）

**路线 A · TerraState**

- [ ] P0：Q3 强基线同协议对照
- [ ] P0：云遮挡、有效历史帧数、预测时域分层
- [ ] P1：未来胁迫检测 + severity / onset
- [ ] P1：冻结 predictive state 的低样本 probe

**路线 B · Stage1 → Stage1.5**

- [ ] P0：严格复核并缓解非线性 acquisition leakage
      （60k 结果**没有**证明完整成像不变性：线性 cross-covariance 约束保持较低，但非线性 MLP probe 仍能恢复部分 orbit/satellite 信息）
- [ ] P1：跨模态补全 / 云鲁棒
- [ ] P1：一个分类 + 一个像素级任务的冻结 / 少样本迁移

**明确不做**（89 号文档冻结）

- 不为凑任务数给 TerraState 硬塞无关目标检测
- 不把条件天气模拟写成因果反事实
- 不把 Q2 / Q3 降格为可有可无的附属消融
- 不把 Stage1–1.5 的候选状态宣传成已完成的世界状态
- 不在当前 direct-horizon 接口上伪造 Q4 composition

### P2 · 收尾

- [ ] 确认新结构无误后清理本地回滚 tag：`git tag -d prerestructure/{main,plan-a-vits,plan-b-pvt,plan-b-v2-train}`
- [ ] 决定陈旧分支 `agent/obsworld-aaai27-planning`（落后 48 个 commit、领先 0）是删还是留

---

## 三、环境与操作要点（踩过的坑，别重复踩）

- **这台工作机没有 `gh` CLI**。PATH、所有 conda env、`/usr`、`/opt`、`/home`、`/mnt/data/public_tools` 都翻过，唯一命中是 `/usr/share/X11/xkb/symbols/gh`（键盘布局文件，无关）。
  仓库里的 `obsworld/scripts/upload_weights_via_github.sh` 第 24 行是 `command -v gh || exit 1`，**在这台机器上跑不了**——它是给训练服务器写的。
  替代方案：`curl` + 一个 `repo` scope token 走 REST API 就够，不需要 gh。
- **本地代理 `127.0.0.1:7890`（mihomo）会中途断**。1.42 GB 上传时传到第 5 个文件就挂了。大文件传输必须带 `-C -`（断点续传）与 `--retry`。
- **GitHub Release 断掉的资产会停在 `state=starter`**：按名字查得到，但实际不可用。仅按名字判断「已存在」会误跳过——**必须同时看 `state == 'uploaded'` 和字节数**。修法：删掉残骸再重传。
- **`.gitignore` 的 `*.csv` / `*.log` / `checkpoints/` 会误伤归档证据**。已在根级 `.gitignore` 加了 `!*/archive/**` 等例外；新增证据目录时注意复查 `git status --ignored`。
- 两个项目**不要跨目录 import**。`terrastate/` 已验证 56 个 `.py` 零跨目录依赖，请保持。

---

## 四、速查

```bash
# 克隆（训练端）
git clone git@github.com:fuchen0614-oss/WorldModel2026.git WorldModel2026v2
cd WorldModel2026v2 && git checkout main

# 权重就位后自检
python - <<'PY'
import hashlib, pathlib
W = {
 "terrastate/checkpoints/contextformer_official/contextformer6M/seed42.ckpt":"ec6706e8a904bba8a195d542921f54c6ce058f8d0d7a9aaeb91f117237d4a4fa",
 "terrastate/archive/07_WEIGHTS_AND_PROVENANCE/historical_boundary80_release/checkpoint_boundary80.pt":"644deaac0b1578cd153eaffb65bddd6c5ac55d30e0bc09b595111588471e1acd",
 "terrastate/archive/07_WEIGHTS_AND_PROVENANCE/phase1_b4_teacher/checkpoint_best.pt":"2c5d084236716d84d1ed11289248a501a7cb906675a32ccb8fd73e1f2a26881c",
 "obsworld/archive/07_WEIGHT_PROVENANCE/weights/stage1_5_final_state_bridge/checkpoint_step_60000.pt":"24646b89eda5fb97ff03a76da5c136969bd1e2af9d76d60bd9537b6e304ff97d",
 "obsworld/archive/07_WEIGHT_PROVENANCE/weights/stage1_final/checkpoint_epoch200_step_95000.pt":"79b20ee6ddc499c60019ed8590108e08789dcc0d8877d1892eb490b2cc5500df",
 "obsworld/archive/08_STAGE2_CONTINUATION/03_KEY_WEIGHTS/plan_a_s1a_full24/checkpoint_best.pt":"2a0a465fe4d4a148a493954a8acc63b0e6e55896b12631cf3bd9efa08440fad5",
 "obsworld/archive/08_STAGE2_CONTINUATION/03_KEY_WEIGHTS/rollout_physical4/checkpoint_best.pt":"8908c62e40b6f71d7ca45aa74047ba2e0c719ea4bc2db7d9cef41587510ead31",
 "obsworld/archive/08_STAGE2_CONTINUATION/03_KEY_WEIGHTS/direct_physical4/checkpoint_best.pt":"1158ffe6644e6a05345cba3fa56ee73af8d1390a2eb078b4b0bc3a94746f91d2",
}
ok = 0
for p, exp in W.items():
    f = pathlib.Path(p)
    if not f.exists():
        print(f"缺失   {p}"); continue
    h = hashlib.sha256()
    with open(f, 'rb') as fh:
        for b in iter(lambda: fh.read(1 << 22), b''): h.update(b)
    d = h.hexdigest()
    print(("一致   " if d == exp else "哈希不符 ") + p); ok += d == exp
print(f"\n{ok}/8 就位")
PY

git status --porcelain   # 应为空；权重不应出现在这里
```

**关键入口**

| 用途 | 路径 |
|---|---|
| 研究侧总纲（双线优先级） | `obsworld/思路整理进展/89_双线推进_...20260813.md` |
| TerraState 最新执行口径 | `terrastate/思路整理进展/88_TerraState_full24唯一条件合同_...20260726.md` |
| TerraState 证据表 | `terrastate/TERRASTATE_V2_EVIDENCE.md` |
| 已提交正文 / 补充材料 | `terrastate/submission/main.pdf`、`.../aaai27_supplement/supplementary.pdf` |
| Q3 热旱 84 对冻结清单 | `terrastate/artifacts/protocols/extreme_audit_oodt_v1/` |
| ObsWorld 从归档续训 | `obsworld/archive/00_START_HERE/RUN_FROM_ARCHIVE.md` |
| ObsWorld 结论边界 | `obsworld/archive/00_START_HERE/RESULT_TRUTH_AND_LIMITATIONS.md` |

---

## 五、本文件的维护约定

训练端接手后请**直接在本文件上追加与勾选**：

- 完成的项把 `- [ ]` 改成 `- [x]`，并在行末补上**实测结果**（路径、SHA-256、耗时、失败原因）
- 新发现的问题追加到「二、待办」对应优先级下，标注发现时间
- **不要删除**已记录的负结果与边界声明——它们是后续设计必须正视的事实
- 跨天时新开一节 `## 08-16` 而不是覆盖本节
