# 有意排除的内容

以下内容没有进入精选归档，但仍保留在原项目中：

- `*.aux`、`*.log`、`*.fls`、`*.fdb_latexmk`、逐轮 compile log；
- 旧 Figure v1/v2/v3、wireframe、近似渲染、重复 preview 和中间 contact sheet；
- `.paper-review/` 多轮过程材料和已被最终审计覆盖的 pre-audit；
- 本地 TeX、Poppler、Tectonic、TinyTeX 等工具链；
- `WorldModel2026-planb*` 的重复副本；
- 失败方案、旧工程标签、旧 Table panel 和已经放弃的 Q4/composition 叙事；
- Stage 2 早期权重与本论文最终模型无关的 checkpoint；
- 数据集本体、cache、manifest 构建中间物和训练日志。

排除并不等于删除。需要追溯时仍可回到原目录；本目录只负责提供一个低噪声入口。

