# 图6｜端点监督与分段稳定性

用途：A 检查 C0R 与 C1 的配对 RMSE 差；B 检查九种 C1 分段方案在 development 与独立 IID confirmation 上的稳定性。

注意：A 的负值表示 C0R 更低；B 的数值是 MSE 比值，不能与 A 相减。图例已放入对应 B 子图的留白区并使用白色背景。

正式图在 `selected/`，冻结统计在 `data/`，重绘见 `code/README.md`。
