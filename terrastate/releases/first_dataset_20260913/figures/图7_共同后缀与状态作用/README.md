# 图7｜共同后缀与状态作用

用途：综合展示四划分逐时距轨迹、receiver等权效应、分布与状态—输出关联。

整张图采用2×2排版：A、B位于第一行，C、D位于第二行。A本身只保留一个坐标系，在其中汇总四个划分的D20、A、B与state donor轨迹（共16条线），不再把A拆成四个小图；曲线右端直接标注划分，图例留在A内部。D面板把按状态距离排序后相邻10个receiver的中位数浓缩成一个点；Spearman ρ仍由原始receiver计算。A内2×2旧版、原始散点与八等频分箱版本保存在完整图件源包的`alternatives/`，本发布精简包只收录终稿。

正式总图与机制主例M05在`selected/`；候选库在`candidates/`；图注见`CAPTION_ZH.md`，完整读图分析见`ANALYSIS_ZH.md`，重绘见`code/README.md`。
