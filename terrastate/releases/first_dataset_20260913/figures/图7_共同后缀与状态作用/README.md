# 图7｜共同后缀与状态作用

用途：综合展示四划分逐时距轨迹、receiver等权效应、分布与状态—输出关联。

A面板内部采用2×2小图，每格只绘制一个划分的D20、A、B与state donor四条线；先前的单图16线版本保存在`alternatives/`。D面板把按状态距离排序后相邻10个receiver的中位数浓缩成一个点；Spearman ρ仍由原始receiver计算，原始散点与八等频分箱版本也在`alternatives/`。

正式总图与机制主例M05在`selected/`；候选库在`candidates/`；图注见`CAPTION_ZH.md`，完整读图分析见`ANALYSIS_ZH.md`，重绘见`code/README.md`。
