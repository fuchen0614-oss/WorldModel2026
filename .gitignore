# ============================================================
# WorldModel2026 — 仓库级忽略规则
# 两个项目 terrastate/ 与 obsworld/ 共用本文件。
# 原则：排除权重、运行时产物与缓存；但 archive/ submission/
#       evaluations/ 属于策展证据，必须完整入库。
# ============================================================

# ---------- 模型权重：一律走 GitHub Release ----------
# 见 terrastate/WEIGHTS_INDEX.md 与 obsworld/WEIGHTS_INDEX.md
*.pt
*.pth
*.ckpt
*.bin
*.safetensors
*.onnx

# ---------- 运行时输出 ----------
logs/
outputs/
wandb/
tensorboard/
runs/
/output/

# ---------- 大体量中间数据 ----------
*.log
*.csv
*.parquet
*.h5
*.hdf5
*.nc

# ---------- Python ----------
__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
dist/
build/
.Python

# ---------- 虚拟环境 ----------
venv/
env/
.venv/
.conda/

# ---------- 系统与编辑器 ----------
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# ---------- LaTeX 中间产物 ----------
*.aux
*.fls
*.fdb_latexmk
*.synctex.gz

# ---------- 迁移临时目录 ----------
_UPLOAD_STAGING/
_UPLOAD_TMP/
_MAIN_WT/

# ============================================================
# 例外：策展证据必须入库
# 上面的 *.log / *.csv 等是为训练产物写的，不适用于归档树。
# 注意 git 规则：只能用文件模式取消忽略，父目录不可先被整目录排除。
# ============================================================
!*/archive/**
!*/submission/**
!*/evaluations/**
!*/checkpoints/**
!*/reports/**
!*/results/**
!*/artifacts/**
!*/refine-logs/**
!*/writing/**

# 但归档里的权重仍然排除（Release 承载）
*/archive/**/*.pt
*/archive/**/*.ckpt
*/archive/**/*.pth
*/archive/**/*.safetensors
*/checkpoints/**/*.pt
*/checkpoints/**/*.ckpt

# 预测 NetCDF 体量过大，任何位置都不入库
**/predictions/
*.nc

# 本地备份目录，不入版本库
terrastate/eval/_backup/
